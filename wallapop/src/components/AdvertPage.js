import Button from "../shared/Button"

const AdvertPage = () => {
    console.log('Página de anuncios')
    return (
        <>
        <section>Aquí el listado de anuncios</section>
        </>
        /*
        <a href="/addDetail.html?addId=${product.id}">    
            <div class="add">
                <div class="productInfo>
                 
                    <div class="productName">
                        <h1>${product.name}</h1>
                    </div>

                    <div class = "productData">
                        <p> Se <span id="isSale" >${product.select}</span> este producto por:</p>
                        <h2>${product.price} €</h2>
                        
                        <div class = "typeTag">
                            
                        </div>
                    </div>

                    <div class="product-img">
                        <img src="${product.photo}"> 

                        <p>Fecha de publicación:<br>
                        <span>${addDate.toUTCString()}</span>
                        </p>

                    </div>
                </div>
            </div>
        </a>
        */
    )
}

export default AdvertPage